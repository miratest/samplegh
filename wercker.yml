box: debian
build:
    steps:
        - script:
            name: Install modules
            code: cpan Mira
        - script:
            name: Run mira
            code: mira build
deploy:
    steps:
        - lukevivier/gh-pages:
            token: $GITHUB_TOKEN
            basedir: public
