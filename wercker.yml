box: debian
build:
    steps:
        - install-packages:
            packages: curl
        - script:
            name: Install cpanm
            code: curl -L https://cpanmin.us | perl - --sudo App::cpanminus
        - script:
            name: Install Mira
            code: cpanm -n Mira
        - script:
            name: Run mira
            code: mira build
deploy:
    steps:
        - lukevivier/gh-pages:
            token: $GITHUB_TOKEN
            basedir: public
