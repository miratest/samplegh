box: perl
build:
    steps:
        - script:
            name: Install modules
            code: cpanm -n Mira
        - script:
            name: Run mira
            code: mira build
            deploy:
  steps:
    - gh-pages:
        token: $GIT_TOKEN
        domain: https://miratest.github.io/samplegh/
        basedir: public
