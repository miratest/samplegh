box: perl
build:
    steps:
        - script:
            name: Install modules
            code: cpanm -n git://github.com/kiamazi/mira.git
        - script:
            name: Run mira
            code: mira build
