box: debian
build:
    steps:
        - script:
            name: Install cpanm
            code: curl -L https://cpanmin.us | perl - --sudo App::cpanminus
        - script:
            name: Install modules
            code: cpanm -n Mira
        - script:
            name: Run mira
            code: mira build
deploy:
    steps:
        - lukevivier/gh-pages:
            token: $GITHUB_TOKEN
            basedir: public
